# 开发决策日志

## 2026-08-31

- D001：本轮正式进入工程阶段。`项目现状.md` 中“当前阶段不写工程骨架”的旧阶段说明由本轮用户指令覆盖；产品边界和 Agent 规则继续生效。
- D002：LLM 主 provider 为 `gpt-5.6-sol`，备用为 `kimi-k3`。`架构.md` 中“LLM 选择待定”已过时，后续 provider 适配以本轮确认内容为准。
- D003：下载到本机的 AADR v66.p1 `.anno` 实测为 49 列、23,089 行数据；用户 prompt 和既有调研报告中的“48列”与文件不符。导入器按源文件的 49 列处理，并保留源文件 hash。
- D004：AADR `Individual ID` 用于把同一 release 中的多个遗传数据表示归到内部 person UUID；`Genetic ID` 与 `Persistent Genetic ID` 只进入遗传记录及 ID 映射表。跨数据源的人物合并留给后续人工确认，不自动猜测。
- D005：M1 第一条数值链路使用明确标记为 `fixture` 的小矩阵验证算法和制品格式；fixture 不进入真实展示。取得真实 PLINK 基因型并完成基准对照后，使用同一构建入口替换数值制品。
- D006：现代参考人群名单作为构建输入，不在代码中固定地区或人群。首发区域和展示章节继续保持未定。

- D007：PCA 使用独立的 `AADR_v66_p1_HO` 包。`AADR_v66_p1_1240K` 中的现代样本主要为 shotgun／WGS，不能作为 Human Origins 数据冒充 HO 底板。
- D008：M1 真实数值基准使用区域无关的稳定哈希抽样：每个群体最多一人，排除 AADR 已标离群组；256 个现代 HO 参考、64 个古样本投影、20,000 个经现代参考 QC 与 LD pruning 的 SNP。该清单只验证数值管线，不构成首发章节。
- D009：EIGENSOFT 8.0.0 对 packedped 的 `FID:IID` 有 39 字符限制。smartpca 工作副本使用短别名，完整 Poseidon ID 保存在双向映射中；原始数据和 catalog ID 不改写。
- D010：NumPy 古样本投影采用逐样本共同观测 SNP 的 least-squares projection。与 smartpca `lsqproject` 对照后，古样本投影 R² 为 0.99997、六维距离相关为 0.9999995，因此保留为等价快速实现。
- D011：M1 已完成并验收；当前 `artifacts/numeric` 状态为 `published`，fixture 已退出真实制品路径。

- D012：独立考古证据与历史地名子集尚未注入；对应工具返回 `no_data` 或 `unknown_place`，不把 AADR 出土地点包装成独立考古证据。
- D013：六件工具统一通过 provider-neutral JSON 参数调用；FastAPI 只负责传输，调查 Agent 后续直接复用同一 `EvidenceTools.execute`。
- D014：M2 已完成；8 个本地测试和 4 个 HTTP 冒烟请求通过。
- D015：调查循环只消费 provider-neutral 的 `AgentAction`；MockLLM 的调查动作由 fixture 提供，业务循环没有预写调查路径。
- D016：SSE 会重放会话内已产生的事件，避免 POST 创建调查后 EventSource 稍晚连接而丢失开头。
- D017：M3 已完成；M2+M3 共 11 项测试通过，HTTP 联调收到从开始到完成的 17 个 SSE 事件。
- D018：M4 仅实现 Canvas 主视图；地图继续保持可选，不引入 MapLibre 依赖。
- D019：M4 已完成；Vite 生产构建、1 项前端回归测试、11 项后端测试和经 Vite 代理的完整 17 事件 SSE 路径通过。浏览器自动检查因本轮浏览器运行环境未能启动而未执行，页面已在 Codex 右侧面板打开供人工查看。
- D020：研究 Agent 只接收同时带 `quote` 与 `source_url` 的结果；通过者写入独立 `research_staging.sqlite`，状态固定为 `temporary`，不直接转正到主 catalog。
- D021：研究 Agent 的 `no_data` 与缺引文 `rejected` 使用不同事件；任何研究返回都不自动消除原始证据暗枝。
- D022：改方向沿用同一 investigation ID 和递增 SSE 序号；已完成会话从 `after` 游标续流，旧证据树不清空。
- D023：M5 已完成；15 项后端测试、前端构建与回归测试通过，HTTP 联调验证 20 事件双 Agent 首轮和 19 事件增量改方向路径。
- D024：厂商模型只在 adapter 层出现；业务循环继续消费 `agent/PROTOCOL.md` 的三类 canonical JSON action。原生 `tool_calls` 和纯 JSON 模型输出都会翻译到同一结构。
- D025：`auto` 模式只在 base URL、key、model 三项齐全时启用对应远程 provider；没有 key 时自动使用 MockLLM，不阻塞启动。模型名使用用户给定配置位，实际 API model ID 由环境变量覆盖。
- D026：现场保底为显式 `/api/replays` 路径，复用 MockLLM fixture、同一六件工具、研究 Agent 和 SSE；不把 fixture 静默混入远程调查。
- D027：主备 adapter 已通过本地 OpenAI-compatible HTTP 模拟、原生 tool-call 翻译与 failover 测试；外部 provider 实 key 联网验收等待用户提供 key。
- D028：M6 本地工程路径完成；累计 19 项后端测试、1 项前端回归、Vite 生产构建、一键启动健康检查及 20 事件保底重放通过。
- D029：产品名称由“迦罗树”改为“光锥之内”；前端、HTML 标题、FastAPI 标题、Agent system prompt、README、启动提示和前端包名同步更新。
- D030：新配置字段使用 `LIGHTCONE_*`；运行时继续兼容旧 `JIALUO_*`，已生成 UUID 的内部 namespace 与现有 Conda 环境路径保持稳定。OpenAI 与 Kimi 默认模型 ID 分别使用官方 `gpt-5.6-sol`、`kimi-k3`。
- D031：“光锥之内”改名与 `LIGHTCONE_*` 配置完成后，20 项后端测试、1 项前端测试和生产构建通过。
- D032：前端默认提交动作改为创建独立 investigation；“沿本次继续”保留同一会话上下文。后端提供会话列表，页面可逐条回看单次事件流。
- D033：“停止/停止调查/终止/stop”等作为会话控制指令处理，发布 `investigation.stopped`；控制指令不写入树根，不交给 LLM 当调查对象。
- D034：研究 Agent 在 Kimi key 可用时接入官方 `moonshot/web-search:latest` Formula；仍以 provider-neutral `ResearchOutcome` 回流。无 Kimi key 或显式 `LIGHTCONE_RESEARCH_MODE=mock` 时使用本地 fixture。
- D035：`mark_evidence_gap` 是最终显式空白，不再重复触发研究 Agent。其他本地缺口的研究结果写回调查 Agent observations；主 Agent 收束前至少再读取一次已完成的研究结果。
- D036：按地点查询 AADR 时必须提供 `min_bp/max_bp`，或在用户明确要求跨时代覆盖时传 `time_scope=all_periods`，阻止历史人物问题误召回同地史前样本。
- D037：前端隐藏开发用保底重放按钮，保留 `/api/replays` 后端应急路径；右侧原始 SSE 列表改为按每次工具检索组织的内容、参数和结果卡片。暗枝只显示显式 `evidence_gap`，不重复显示每个空工具结果。
- D038：本轮修复通过 23 项后端测试、1 项前端测试和 Vite 生产构建。
- D039：真实“曹操”验收中，时代约束与 Kimi Formula 搜索均生效，但主 Agent 与研究 Agent 并发使用同一 Kimi key 时第三次主请求收到 HTTP 403。所有远程调用改为共享队列；调查循环在研究结果回流后再规划下一步。终止状态按最近的终止事件判定，迟到研究事件不能把历史状态改回 running。
- D040：第二次“曹操”验收遇到 Kimi Formula `RemoteDisconnected`，随后主模型在原 45 秒上限超时。Kimi K3 调查请求改用 `reasoning_effort=low`，通用远程超时提高到 90 秒；主模型与研究 Formula 对断连、超时、429 和 5xx 各做一次有限重试。
- D041：重启后的“曹操”真实验收完整通过：Kimi 调查 Agent 与官方 `web-search` Formula 返回 10 条带引文临时证据，时间范围保持在东汉相关语境，明确区分曹操本人数据缺口与曹鼎家族古 DNA，24 个事件后状态为 `completed`。右侧调查过程在运行时只保留最新阶段叙述，完成后只显示最终结论，避免重复总结刷屏；24 项后端测试、2 项前端测试和 Vite 生产构建通过。

## 2026-09-01

- D042：联网研究准入从“引文+URL”提高为“学术来源+原文引文+直达地址”。接收同行评议论文、正式发掘报告、学术专著、学位论文、科研机构仓储、权威数据库和博物馆目录；新闻、门户转载、自媒体、百科与聚合页在入树前拒收。
- D043：研究结果新增 `claim`、`relation_to_question`、`source_kind`、作者／刊物／DOI、`event_year_start/end` 与 `narrative_role`。前端以这些字段生成按历史时间排序的故事脉络，调查工具日志降为可折叠轨迹。
- D044：时间轴使用事件年份；古样本 `mean_bp` 按 `1950 - BP` 转为公历位置；缺少可靠事件年代的证据进入“年代待定”区，不再按数组序号随机排布。25 项后端测试、4 项前端测试和 Vite 生产构建通过。
- D045：真实“曹操”复测发现调查 Agent 已把查询缩小为 `Cao Cao Y chromosome Fudan`，但外派研究任务只携带主问题与工具名，丢失了本轮工具参数，导致研究 Agent 重复泛化搜索。`_research_gap` 现将完整 JSON 工具参数写入研究任务，回归测试确认 `Loschbour` 等具体检索词能穿透到外派查询；25 项后端测试通过。
- D046：下一次真实复测中 Kimi 首轮返回普通文本，暴露 adapter 虽能解析原生 `tool_calls` 却没有向模型发送工具定义。OpenAI-compatible adapter 现把六件证据工具及 `narrate_investigation`、`finish_investigation` 转为原生 function schema；Kimi 与 GPT 模型使用原生调用，adapter 再翻译为 canonical action。26 项后端测试通过。
- D047：原生函数接入后的“曹操”真实调查完整通过。研究 Agent 只返回 3 篇学术论文（复旦论文 PDF、Journal of Human Genetics/Nature 页面、《人类学学报》），形成“现代家系反推→曹鼎古 DNA 验证→下游 SNP 精细分型”的连续证据链，10 个事件后状态为 `completed`。故事卡片保留 Agent 给出的叙事顺序，历史锚点显示完整年代区间，并把论文发表年份单独标为证据时间；5 项前端测试和 Vite 构建通过。
