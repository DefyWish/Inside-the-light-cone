# AGENTS.md — 光锥之内

> 任何 AI（包括新会话的自己）动手前必读：本文件 + `项目现状.md`。
> 冲突时以 `项目现状.md` 中用户确认的内容为准。状态只写进 `项目现状.md`，不另建状态文件。

## 产品本质（用户原话收敛）

给一个问题，Agent 自循环自驱动；后端证据链 = **古基因组 + 考古证据**（并列第一）+ 文献综述（辅助）；前端 = 数字人文（落叶归树的时空树）；产出有证据的历史故事。**它是调查员，不是搜索引擎；树是调查过程的外化，不是渲染结果。**

## 红线（用户确认，不得违反）

- 有证据才说，没有就明说（暗枝），绝不编造人物/关系/年代/引文/血缘/百分比
- view 永远低于 fact；论文模型必须带归属，交用户判断
- 不做成分百分比；不做表演化脚本；路演实例必须有公众认知入口（"曹操"级），北阡/喇家类冷门选题已否决
- 只用公开数据；非公开数据不入库、不进仓库
- Agent ≠ workflow：调查路径由模型现场决定，代码不得预写路径（MockLLM 是唯一的例外，用于保底重放）

## 架构（已定，勿改方向）

- 后端 Python+FastAPI（SSE）｜前端 React+Vite，树动画 Canvas 自绘（`frontend/src/TreeCanvas.jsx`）
- 数据：AADR v66.p1（CC0）为基因骨架；ID 三层（人/基因数据版本/跨版本稳定ID）；制品 `artifacts/catalog.sqlite` + `artifacts/numeric/`
- 双 Agent：调查Agent（探索循环+六件工具）｜研究Agent（异步外派，引文为准入门槛，无引文拒收）
- LLM：provider 抽象（主 gpt-5.6-sol / 备 kimi-k3），key 走环境变量，无 key 用 Mock
- 生信管线离线跑，现场只做毫秒级查询；每条遗传结论带方法注记

## 当前工程优先级（2026-09-01 定，9.5 路演倒排）

- **P0** claim/relation 最小闭环：DB 写入 + `claim.added`/`relation.added` SSE 事件 + 前端按关系分叉/汇合（用户9.1确认：保留"根部长叶子"生长动画，只改叶子平铺无分叉，不做视觉方向大改）
- **P1** 研究Agent 真异步（删每轮开头的 gather，只合并已完成结果）+ 收束完整性检查
- **P2** research_staging 回读 + FTS5 文献检索 + 中文别名层（手工录入路演题映射，不做通用NER）
- **P3** 引文子串核验、AADR亲缘字符串解析、fact/view 细化

## 代码现状诊断（2026-09-01 逐卷亲验，勿重复体检）

**总判决：骨架合格，禁止重写。** 4700行、模块清晰、provider 抽象带故障转移、research_agent.py 的引文准入实现良好。唯一核心病灶：**全栈缺"关系层"**——这是"曹操"测试退化为普通搜索摘要的根因。

已验证病灶（按文件定位）：

1. `evidence_edges` 表存在但 0 行、全代码库无 INSERT → claim/relation 需新建运行时存储
2. `providers.py` `InvestigationState` 原来只有 question+observations，模型无"棋盘"可推理 → P0 已加 claims/relations 字段
3. `investigation.py` 每轮开头 `await gather(全部研究任务)` → 假异步，P1a 修
4. 文献工具只 LIKE 简称/DOI；`research_staging.sqlite`（41条）无任何代码回读 → P2 修
5. AADR 亲缘字段 raw 字符串未解析 → P3 修
6. `evidence_tools.execute` 的 `except Exception → no_data`：故障与真空白不分 → P2/P3 引入 `unavailable`
7. **前端叶子平铺无分叉**：`TreeCanvas.jsx` 叶子 hash 决定左右、彼此无关系；需按 relation 分叉/汇合（生长方向"根部长叶子"经用户确认保留，勿再提议大改视觉方向）
8. 考古/地名两工具是纯桩 = 数据未策展（正常，非代码问题）

对开发AI报告的采信度：事实清单约85%属实（以上1–6即其内容），但"全面失败"系夸大；其九步医嘱中"跨场景验收矩阵"是过度工程，已否决。

## 工作纪律（前任 AI 被开除的原因）

1. 最小实现。禁止脚手架/校验器/契约文档/验收矩阵的增殖
2. 不替用户发明需求；例子保持例子；不确定就列出来问
3. 每处改动给出可运行结果
4. "无数据"是正常分支，不是异常；但系统故障与真空白要可分（unavailable ≠ no_data）
