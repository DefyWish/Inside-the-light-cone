# 项目现状

> 唯一状态文档。只记录用户本人表述，按状态分层。
> 最后更新：2026-09-01（学术来源准入、故事脉络与时间轴修复完成）

## 已确认的事实

- 产品名称（用户 8.31 确认）：**光锥之内**（立意：光锥之内即命运——把过往事件的光锥串成因果/命运的网络，故产品天然不限于考古，可推广至情报分析）
- 团队名称（用户 9.1 确认）：**劫波事务所**（双关：梵语 kalpa=时间/劫波；情报分析中"劫取信息"之"劫"）

- 赛事节奏（用户8.30更正）：**真正的项目工作8.30即可开始，从零起步约一周**；9月5–6日为深圳线下活动，性质是最终冲刺和项目路演，不要把开发拖到9.5
- 主题"全域融合 & Agent+创意"
- 路演现场**允许联网**（用户8.30确认）；但现场网络质量未知，仍建议保留轻量保底方案
- 受众：投资人与公众；目标是"眼前一亮"；学术性要求低；不做窄而专的课题
- 团队（用户8.30两次更正、9.1补充）：**用户 + 队友两人**。队友本硕博为 biotech/药物合成背景，博后转分子人类学（首博+北大联培），负责数字人文内容策展、可视化、设计；她无coding/数据库基础，工程方面不指望。无第三人。**医药情报方向因此团队内部无知识缺口**
- 数据红线：只用公开数据，不用涉密内部数据
- 产品红线：不做伪精确成分百分比式报告
- 架构要求（用户8.30）：。Agent 的数据接口尽量按"开放大海"设计，最终目标是放归大海而非鱼塘测试集；即使9.6路演实现不了完全体，架构必须为此留好接口。禁止"问个别问题就原地宕机"的死板实现。

## 用户提出的方案（当前讨论主体）

"故事展开机"：前端是一棵树，数据层允许枝条重新汇合。古基因组只负责提供"谁在何时何地出现、彼此有什么关系、研究者据此提出了什么历史模型"。每次点击同时改变地图、时间轴和 Agent 叙述。

## 用户确认的 Agent 机制（8.30）

**核心定义（用户原话收敛）**：给一个问题，Agent 自循环自驱动，后端证据链 = **古基因组 + 考古证据 + 文献综述**，前端 = 数字人文（时空地图等），产出漂亮的结果，讲述历史故事。

1. **目标驱动的探索循环**：用户给一个种子（地点或样本编号），Agent 带任务自主规划探索路径，每一步"接下来查什么"由它现场决定，不是脚本
2. **好奇心可见化**：新枝条挂 Agent 的动机标签（第一人称工作日志）；Agent 有偏好、会起疑、会放弃；树是"被探索过的"，不是"被渲染的"
3. **用户可夺方向盘**：观众随时打断并重新定向，Agent 现场重新规划，树因每个观众而不同
4. **多模型呈现交用户判断**：面对论文中并存的模型，Agent 摆出各派证据，交由用户判断

**证据优先级（用户8.30修订）**：**古基因组 AND 考古证据 = 并列第一证据**，其余（文献综述、论文模型争议、数字人文数据库等）为辅助。view 永远低于 fact。另外：古基因组是团队壁垒所在，要充分体现 bioinformatics 工作量。

**路演方式（用户8.30）**：用一两个精心挑选的实例做展示，但拒绝表演化编排——这是投资路演，不是网红短剧

**路演选题原则（用户8.31确立）**：实例必须有公众认知入口（"曹操"级别），**绝不**按"数据库里哪里样本多、亲缘多"来选题——数据密度服务于选题，不决定选题

**前端可视化方向（用户8.30）**：两张图结合——
- **主视图（必须有）**：抽象布局，"反方向的钟"+"落叶归树"——散落在今天的残迹（样本、地名、器物）逆着时间升起，飞回枝头重新变芽，树枝逐段点亮，树状回溯历史。叶子飞回枝头 = Agent 查实一份证据；两枝合流 = 人群相遇；始终暗着的叶子 = 无法确证的留白
- **副视图（可选，视数据情况而定）**：地理空间分布视图。数据缺失就不要，只做主视图
- 原则：服务于 Agent 主线，不做与调查无关的装饰动画；拒绝表演化

## 待定（用户未拍板）

- 首发区域：未确认
- 路演时长：10 分钟（已确认）；demo 叙事结构待定

## 已确认（续）

- LLM 选型（用户8.30）：第一选择 `gpt-5.6-sol`，第二选择 `kimi-k3`（均使用官方 API model ID）

## 已确认的技术决策（8.30）

- 后端：Python + FastAPI（SSE 向前端推事件流；Agent 循环、生信计算、数据查询同一进程）
- 前端：React + Vite 单页；落叶归树主视图 Canvas 自绘；地理副视图 MapLibre（可选）
- 生信管线：离线跑（AADR基因型→转换质控→PCA投影→距离矩阵→亲缘分析），现场只保留毫秒级实时查询
- 架构三层：数据层（开放接口，镜像/在线可互换）→ Agent 层（探索循环+六件工具）→ 前端层（事件驱动生长）
- **双Agent现场架构（用户8.30确认）**：调查Agent守本地数据；撞到证据缺口时异步外派研究Agent现场检索文献/网络，考证结果带回后枝条点亮；外派失败退回标空白；核心路径不依赖现场检索
- **现场考证的呈现纪律（用户8.30确认）**：前台不做"未核验"降级标注；唯一门槛是必须带引文原文，无引文的结果调查Agent拒收；来源标记只在后台保留（用于赛后人工回流审稿）

## 保持开放的可能性（脑洞层，不排期、不否决）

- HLA / SNP / CNV / 表观组的"幸存者"叙事 → 未来接口
- 个人基因数据接入 → 未来接口
- 博物馆装置、文旅等落地形态
- "暗物质"资源钩子（用户8.30）：架构预留**通用古基因组数据接入口**——不限于某机构，日后任何公开或私有的古基因组测序数据（新发表论文、机构未公开样本，如明清华北墓葬数据）都应能作为新数据源接入。路演禁用任何非公开数据、不入库、不进代码仓库
- 情报分析衍生方向（用户9.1脑洞）：Agent的证据图本质=开源情报分析的证据评估图（claim/分级=评估，contradicts=ACH竞争假设，暗枝=情报缺口）。域无关架构可迁移：医药靶标情报（首选，用户有专利药检索Agent经验）、企业尽调、病原体进化（注意Nextstrain已占"进化地图"生态位）
- 系统生物学定量层钩子（用户9.1脑洞，中远期）：证据图=PPI式带权网络，可迁移图论工具——中心性→承重墙claim识别（量化好奇心）、富集分析→单一来源citogenesis告警、网络传播→结构置信度（LLM断言、图论审计）。护栏：分数必须可展开成组成，绝不给无单位神棍分数。边的weight语义留空待用，现有数据模型已可计算，无需改代码

## 已否决（勿再拾起）

- "三重证据法 / 证据断层线"方向
- "姓氏史诗"方向
- 成分百分比式产品形态
- 窄而专的学术课题路线
- 预设脚本的"三幕剧"式路演结构、侦探腔、"庭辩"等表演化设计（用户：做作、假、油腻）
- 北阡、喇家等冷门路演选题（用户8.31：听都没听说过，想都不要想）

## 待验证（未核实信息）

- DORA、Human AGEs 的实际功能与差异化点（在线核实两次均失败）
- 各候选区域的公开古DNA样本密度

## 工作纪律（用户要求的）

1. 状态分层：确认/暂定/脑洞/例子/否决，不得混同
2. 例子保持例子，外部建议保持外部建议
3. 脑洞保持开放，不转为承诺或排除项
4. 产品决策权属于用户；AI 只提供并列选项、指出冲突、推演后果
5. 工程开发已获用户授权；按 M1→M6 分步推进，每个里程碑结束时必须有可运行结果，保持最小实现

## 当前工程进度（2026-08-31）

- M1 已完成：AADR v66.p1 元数据目录、真实 Human Origins PCA／投影基准、共同观测 SNP 距离、重叠位点数和 TopN 近邻制品均已生成并通过验收。
- M1 数值基准只用于验证工具链，采用区域无关的稳定抽样；首发区域和展示章节仍未确定。
- M2 已完成：六件工具、类型化空结果与 FastAPI 调用入口通过本地测试和 HTTP 冒烟检查。
- M3 已完成：provider-neutral 调查循环、fixture 驱动 MockLLM 与 SSE 事件流通过本地测试和 HTTP 联调。
- M4 已完成：React + Canvas 主视图已接通 Mock 调查事件，支持树生长、证据详情、动机日志和空白暗枝。
- M5 已完成：证据缺口异步外派研究 Agent，带原文引文与来源地址才能临时入库；联网结果会回流调查 Agent 的观察上下文。
- M6 已完成 Kimi 主调查 Agent 与官方 Formula `web-search` 的真实 API 验收；“曹操”问题已完成本地缺口识别、联网研究、引文准入、结果回流、古 DNA 边界说明和最终收束。无 key 时仍自动使用 Mock。
- 前端已把新调查、沿本次继续、停止和历史回看拆为独立操作；每条历史记录只显示自己的树与调查过程。
- 联网研究只接收论文、考古报告、学术专著、学位论文、科研机构仓储、权威数据库和博物馆目录；新闻、门户转载、自媒体、百科和聚合页不进入证据树。
- 前端主叙事区按事件年代与叙事角色组织证据，工具调用保留在可折叠的调查轨迹中。时间轴按事件年份或科学测年定位，缺可靠年代的材料单列为“年代待定”。
- 本地 catalog 仍未导入独立考古记录；历史人物与墓葬问题依靠研究 Agent 联网取得带引文的临时结果，后续策展数据可继续注入本地制品。
- 下一步由用户拍板：继续做前端交互验收，或进入首发展示章节与内容策展。

## 当前工程进度（2026-09-01，参谋长接手代码优化）

- `AGENTS.md` 宪法已立：任何 AI 动手前必读，含红线、架构、P0–P3 优先级、代码诊断与工作纪律
- **P0 已完成**：claim/relation 全栈闭环——`providers.py` 新增 claim 动作（evidence_level/links/事件年代）与 InvestigationState 棋盘；`investigation.py` 新增 `claim.added`/`relation.added` SSE 事件、ClaimStore（`artifacts/investigations.sqlite` 运行时持久化，与只读 catalog 分离）；前端 TreeCanvas 渲染 claim 芽、按 relation 分叉/汇合、证据叶挂接关联 claim（"根部长叶子"方向保留，用户确认）
- **P1 已完成**：研究Agent真异步（每轮只清理已完成外勤，不再原地等待，仅收束前合并）；收束完整性检查（无claim且无缺口声明→驳回继续，事件 `investigation.continue_required`）
- 验证：Mock 模式下 SSE 全流程通过（claim/relation/completed 事件齐）；无claim裸收束被正确驳回；前端 vite build 通过；自循环 bug（c1→c1）已修
- 独立代码审查结论：骨架合格不重写；核心病灶=全栈缺"关系层"（已修）；八处病灶定位见 AGENTS.md
- **下一步（P3，赛后或有余力）**：引文子串核验、AADR亲缘字符串解析、fact/view 细化、数值制品扩展到 AADR 全量古样本

## 当前工程进度（2026-09-01 下午，P2 完成）

- **P2a 已完成**：文献工具回读 `research_staging`——"曹操"实测返回别名卡+6条既有研究成果（含复旦曹氏DNA条目），产品开始"越用越聪明"
- **P2b 已完成**：`research_staging` 建 trigram FTS5 索引（含 finding_json 全量字段迁移、新旧行同步）；短词/CJK 两字词自动回退 LIKE
- **P2c 已完成**：中文别名层落地 `data/aliases.json`（人工策展，首张卡=曹操，含高陵/邺城两条 place_history、复旦DNA争议注记）；古样本/文献/地名三工具已接通别名卡
- **真key"曹操"实测通过（kimi-k3，5步收束）**：形成2个claim（考古线 fact_archaeology / 分子人类学线 view_model）+5条关系（含模型自主标注的 contradicts→葛威质疑论文）；主动标记核心缺口"曹操本人遗骸从未做古DNA检测"；收束叙事双线并陈、争议交用户判断——"考古认定与遗传学推断各自指向曹操，却始终未能交叉验证"。研究Agent未外派（staging记忆命中即够用），验证"越用越聪明"
- 待办：队友/用户可继续往 `data/aliases.json` 加策展卡（结构照抄曹操卡）；前端真机联调与视觉打磨；gpt-5.6-sol key 就位后做主 provider 复测

## 当前工程进度（2026-09-02，用法乙实战验收通过）

- **决策1 明暗线升降格已上线并实测**：claim.status（open/strengthened/challenged/dropped）+ claim.updated 事件 + 前端芽亮度语义；Aβ实测中模型自主将"制度性维系"claim 从 open 升格为 strengthened
- **决策2 暗线嗅觉清单已进系统提示词**（四类异常）；决策4 Aβ策展卡v1入库（参谋长起草，标注待医药队友精修）
- **修复"别名卡捷径"缺陷**：文献结果仅含别名卡时标记 alias_only 并触发研究外派；提示词明确"卡是线索不是证据，须外部核实"——此后Aβ调查被迫真查：2轮外派、10条带引文网络来源（PMID/NEJM/EMA EPAR）
- **Aβ真机实测（kimi-k3，9步收束）**：4 claim（六种失败解释/机制性失败累积/制度性维系[strengthened]/脆弱的正名）、13条关系、30条证据、真实PMID引用；收束叙事双线并陈+"缺口至今未被任何试验正面回答"
- 竞品情报（用户9.2提供JSON）：医药情报市场有现役战舰（Causaly/Cortellis/Patsnap），数据壁垒不可硬拼；战略=第2/3层（调查引擎+认识论产品），敌人变水管；WGCNA/GSEA迁移判为"统计可迁移、假设须重建"，稀疏性用降粒度/换方法/飞轮/FDR纪律应对，迁移实验赛后做
- 赛前清单：前端真机联调+美术打磨（队友）、Aβ卡精修（队友）、路演排练、gpt-5.6-sol key

## 当前工程进度（2026-09-02 深夜，明线拆解上线）

- 用户批评"6步3claim是研报废话"后，上线**调查主线（plan/lines）系统**：Agent 开工先拆4-8条主线（医药类：发现史/机制/专利/交易/临床/biomarker/失败机制/监管/竞争/机会；历史类：文献/考古/古DNA/地名/争议），逐线了断（covered/gap）否则收束驳回；claim 必须挂主线且鼓励跨线连线；步数上限16→28；kimi reasoning_effort low→medium
- **Aβ复测（27步收束）**：6条主线全覆盖（起源/失败机制/资金/FDA监管/反方科学/复兴）、6 claim、12关系、**138条证据**（旧版30条）；出现真正的跨线矛盾网：c3失败机制—contradicts→c2 FDA批准、c4复兴—contradicts→c3、c6反方—contradicts→c1/c4；量化情报入叙事（1995–2021私营投入425亿美元、57%烧在III期、235药6获批、A4研究240周随访阴性）
- 前端：主线 chips 面板（covered✓/gap◌）、故事脉络含 claim 金边脊柱并按年份排序
- 修复的关系目标归一化（URL→证据名）使红虚线可渲染
- 前端被队友方做过 i18n 重构（copy 对象），已兼容
